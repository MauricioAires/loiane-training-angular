# Para que serve o Angular CLI

Criar a base do projeto além disso cria também o package.json bastante economia de tempo.

O angular 2 não utiliza mais CDN todas as lib são node-based.

Jasmine (unit) + Protrector + Karma

Muito bom para teste de cross browser.

Já inicia as configurações de commit

embercli (é a ferramenta utilizada para criar o schematics)

- core-js
- reflect-metadata
- rxjs
- ts-helpers
- zone.js

- ng build / ng build --dev / ng build --dev --e=dev / ng build --target=development --environment=dev
- bootstrap
- jqeury
- materialize css
- lodash
