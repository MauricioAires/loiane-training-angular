import { FundoAmarelo } from './fundo-amarelo';

describe('FundoAmarelo', () => {
  it('should create an instance', () => {
    const directive = new FundoAmarelo();
    expect(directive).toBeTruthy();
  });
});
