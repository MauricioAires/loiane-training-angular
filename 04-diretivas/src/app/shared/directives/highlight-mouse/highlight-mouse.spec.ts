import { HighlightMouse } from './highlight-mouse';

describe('HighlightMouse', () => {
  it('should create an instance', () => {
    const directive = new HighlightMouse();
    expect(directive).toBeTruthy();
  });
});
